import 'package:flutter/material.dart';

class NoWeatherDataPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('>> No Weather Data <<')));
  }
}
